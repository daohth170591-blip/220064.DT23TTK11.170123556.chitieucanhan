@model List<FinancialGoal>
@{
    ViewData["Title"] = "Mục tiêu tài chính";
}

<div class="container-fluid mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>🎯 Mục tiêu tài chính</h2>
        <a asp-action="Create" class="btn btn-primary">+ Thêm mục tiêu</a>
    </div>

    <div class="row">
        @foreach (var goal in Model)
        {
            var progress = (goal.CurrentAmount / goal.TargetAmount) * 100;
            var daysLeft = (goal.TargetDate - DateTime.Now).Days;

            <div class="col-md-4 mb-4">
                <div class="card h-100">
                    @if (!string.IsNullOrEmpty(goal.ImagePath))
                    {
                        <img src="@goal.ImagePath" class="card-img-top" style="height: 200px; object-fit: cover;" alt="@goal.Name" />
                    }
                    <div class="card-body">
                        <h5 class="card-title">@goal.Name</h5>
                        
                        <div class="mb-3">
                            <div class="d-flex justify-content-between mb-1">
                                <span>Tiến độ</span>
                                <span>@progress.ToString("F0")%</span>
                            </div>
                            <div class="progress" style="height: 25px;">
                                <div class="progress-bar bg-success" style="width: @progress%">
                                    @goal.CurrentAmount.ToString("N0") / @goal.TargetAmount.ToString("N0") đ
                                </div>
                            </div>
                        </div>

                        <div class="mb-3">
                            <small class="text-muted">
                                <i class="fas fa-calendar"></i> 
                                @if (daysLeft > 0)
                                {
                                    <span>Còn @daysLeft ngày</span>
                                }
                                else
                                {
                                    <span class="text-danger">Đã quá hạn</span>
                                }
                            </small>
                        </div>

                        <form asp-action="AddAmount" asp-route-id="@goal.Id" method="post" class="mb-2">
                            <div class="input-group">
                                <input type="number" name="amount" class="form-control" placeholder="Số tiền" step="1000" required />
                                <button type="submit" class="btn btn-success">Thêm tiền</button>
                            </div>
                        </form>

                        <form asp-action="Delete" asp-route-id="@goal.Id" method="post">
                            <button type="submit" class="btn btn-sm btn-danger w-100" onclick="return confirm('Bạn có chắc muốn xóa?')">Xóa mục tiêu</button>
                        </form>
                    </div>
                </div>
            </div>
        }

        @if (!Model.Any())
        {
            <div class="col-12">
                <div class="alert alert-info">
                    Chưa có mục tiêu nào. Hãy tạo mục tiêu để động lực tiết kiệm!
                </div>
            </div>
        }
    </div>
</div>
